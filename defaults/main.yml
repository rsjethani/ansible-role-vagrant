---

vagrant_download_url: https://www.vagrantup.com/downloads.html

vagrant_release_url: https://releases.hashicorp.com/vagrant

# TODO: Find a better and more secure download destination
vagrant_dest_path: "{{ ansible_env.PWD }}/vagrant_{{ vagrant_version }}"

vagrant_allow_downgrade: no
