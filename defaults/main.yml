---

vagrant_version: 1.9.1

vagrant_pkg_name: vagrant_{{ vagrant_version }}_{{ ansible_architecture }}

vagrant_src_path: https://releases.hashicorp.com/vagrant/{{ vagrant_version }}/{{ vagrant_pkg_name }}

vagrant_dest_path: "{{ ansible_env.PWD }}/{{ vagrant_pkg_name }}"
